{
  "name": "home-server-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:watch": "node build.mjs --watch",
    "test": "node frontend/run-tests.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
